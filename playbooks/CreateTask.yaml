---
- name: Create Task
  hosts: windows
  tasks:
   - name: create new schedule task
     community.windows.win_scheduled_task:
       name: Created Task 1030 am
       description: create new schedule task for infosec
       enabled: true
       actions:
        - path: "C:\\ExecutableSample.exe"
  
       triggers:
        - type: daily
          start_boundary: '10:30:00'
       state: present

